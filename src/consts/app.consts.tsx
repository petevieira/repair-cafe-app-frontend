
const StorageConsts = {
    AUTH: '@auth',
};

const Config = {
    OFFLINE: false,
}

const Regex = {
    SIMPLE_DATE: /^\d{4}-\d{2}-\d{2}$/,
}

export {
    StorageConsts,
    Config,
    Regex,
};